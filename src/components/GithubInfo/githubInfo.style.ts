import { makeStyles } from '@material-ui/core';

const useGithubInfoStyles = makeStyles({
  icon: {
    marginRight: 10,
  },
  text: {
    display: 'inline-flex',
    alignItems: 'center',
  },
});

export default useGithubInfoStyles;
