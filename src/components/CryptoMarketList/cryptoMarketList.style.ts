import { makeStyles } from '@material-ui/core';

const useCryptoMarketListStyles = makeStyles({
  tableCell: {
    display: 'flex',
    flexWrap: 'nowrap',
    alignItems: 'center',
  },
  image: {
    marginRight: 10,
  },
  select: {
    width: 120,
  },
});

export default useCryptoMarketListStyles;
